# Tier 1 — MIT/Apache (import freely)
openalgo>=1.0,<2.0             # MIT SDK (OpenAlgo server is AGPL sidecar)
tradingagents>=0.2.4,<0.3      # Apache 2.0
langgraph>=0.2,<1.0            # MIT (TradingAgents dependency)
nautilus_trader>=1.200,<2.0    # LGPL-3.0 (library use)
pyzmq>=25.0,<27.0              # ZMQ async bindings for OpenAlgo tick feed

# Tier 3 — GPL (patterns only, no source copied)
# Freqtrade: NOT a pip dependency. Patterns reimplemented in feature_pipeline.py.
# QuantConnect LEAN: NOT a pip dependency. Alpha Framework pattern implemented in
#                    alpha_framework.py from scratch (Apache 2.0 concept).
