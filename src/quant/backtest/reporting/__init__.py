"""Backtest reporting — pure functions, no DB.

Modules:
  * metrics  — distributional + risk metrics on a daily-return series.
  * per_arm  — per-arm performance breakdown from a list of trade rows.
  * walk_forward — walk-forward CV harness (Task 12).
  * compare_modes — live-vs-backtest reconciliation (Task 14).
"""
